# name: Build LaTeX report

# on:
#   push:
#     paths:
#       - "report.tex"
#       - ".github/workflows/latex.yml"
#       - "figures/**"
#   workflow_dispatch:

# jobs:
#   build_pdf:
#     runs-on: ubuntu-latest

#     steps:
#     - name: Checkout repo
#       uses: actions/checkout@v3

#     - name: Install LaTeX
#       run: |
#         sudo apt-get update
#         sudo apt-get install -y texlive-full

#     - name: Compile LaTeX
#       run: |
#         mkdir -p output
#         pdflatex -interaction=nonstopmode -output-directory=output report.tex
#         pdflatex -interaction=nonstopmode -output-directory=output report.tex

#     - name: Upload PDF artifact
#       uses: actions/upload-artifact@v3
#       with:
#         name: report-pdf
#         path: output/report.pdf
